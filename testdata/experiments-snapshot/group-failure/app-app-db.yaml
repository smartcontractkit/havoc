
apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: group-failure-app-app-db
  namespace: my-test-namespace
  labels:
    waitLabel: app-app-db
spec:
  action: pod-failure
  mode: fixed-percent
  value: '30'
  duration: 1m
  selector:
    labelSelectors:
      'app': 'app-db'
