
apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: group-failure-app-app-db-2-fixed
  namespace: my-test-namespace
spec:
  action: pod-failure
  mode: fixed
  value: '2'
  duration: 1m
  selector:
    labelSelectors:
      'app': 'app-db'
