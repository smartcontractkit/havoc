
kind: NetworkChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  name: group-latency-app-app-1-fixed
  namespace: my-test-namespace
spec:
  selector:
    namespaces:
      - my-test-namespace
    labelSelectors:
      'app': 'app'
  mode: fixed
  value: '1'
  action: delay
  duration: 1m
  delay:
    latency: 300ms
  direction: from
  target:
    selector:
      namespaces:
        - my-test-namespace
      labelSelectors:
        'app': 'app'
    mode: fixed
    value: '1'
