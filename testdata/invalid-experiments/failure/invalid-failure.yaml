
apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: invalid-failure
  namespace: skudasov-crib
  labels:
    waitLabel: invalid-failure
spec:
  action: pod-failure
  mode: one
  duration: 1m
  selector:
    fieldSelectors:
      metadata.name: invalid-pod-name
