
apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: group-failure-app-app-2-fixed
  namespace: cl-cluster
spec:
  action: pod-failure
  mode: fixed
  value: '2'
  duration: 10s
  selector:
    labelSelectors:
      'app': 'app'
