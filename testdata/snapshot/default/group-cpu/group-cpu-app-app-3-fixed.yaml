
apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: group-cpu-app-app-3-fixed
  namespace: cl-cluster
spec:
  mode: fixed
  value: '3'
  duration: 10s
  selector:
    labelSelectors:
      'app': 'app'
  stressors:
    cpu:
      workers: 1
      load: 100
