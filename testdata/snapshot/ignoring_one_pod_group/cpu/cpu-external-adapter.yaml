
apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: cpu-external-adapter
  namespace: cl-cluster
spec:
  mode: one
  duration: 1m
  selector:
    fieldSelectors:
      metadata.name: external-adapter
  stressors:
    cpu:
      workers: 1
      load: 100
