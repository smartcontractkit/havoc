
kind: NetworkChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  name: group-latency-components-group-group-2-1-fixed
  namespace: cl-cluster
spec:
  selector:
    namespaces:
      - cl-cluster
    labelSelectors:
      'components-group': 'group-2'
  mode: fixed
  value: '1'
  action: delay
  duration: 1m
  delay:
    latency: 300ms
  direction: from
  target:
    selector:
      namespaces:
        - cl-cluster
      labelSelectors:
        'components-group': 'group-2'
    mode: fixed
    value: '1'
