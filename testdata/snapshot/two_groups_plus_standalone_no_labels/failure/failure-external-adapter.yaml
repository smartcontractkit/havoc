
apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: failure-external-adapter
  namespace: cl-cluster
spec:
  action: pod-failure
  mode: one
  duration: 1m
  selector:
    fieldSelectors:
      metadata.name: external-adapter
